name: GitHub Actions Demo
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: npm outdated
      - run: echo “Appending text to the existing text file” >> testfile.txt
